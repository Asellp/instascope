services:
  redis:
    image: redis:alpine
    container_name: instascope_redis
    ports:
      - "6379:6379"
    restart: always
    volumes:
      - redis_data:/data

volumes:
  pgdata:
  redis_data: